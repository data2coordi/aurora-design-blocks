name: "Setup Playwright Environment"
description: "Install dependencies and Japanese fonts (Playwright browsers are provided by container image)"

inputs:
  node-version:
    description: "Node.js version"
    default: "20"

runs:
  using: "composite"
  steps:
    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: ${{ inputs.node-version }}

    - name: Install e2e-test dependencies
      run: echo dummy
      working-directory: tests/e2e-tests
      shell: bash
